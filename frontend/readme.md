Procedimiento por unica vez

git checkout development
git pull origin development
cd frontend
npm install
npx ng serve --open


nota: npm install = Instalar las dependencias del frontend (sólo la primera vez o cuando haya cambios en package.json)